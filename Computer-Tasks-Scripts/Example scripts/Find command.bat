@echo off

echo Find batch command

rem find a string or text that dosent exist in the file

find "Bryan" "C:\Users\brill\OneDrive\Escritorio\HENNA\New Text Document (2).txt"

rem it will display the whole line were the string is located
rem if string dosent exist, there is no output
rem if string is repeated in many lines, does lines will be outputed
rem is case sensitive, so it has to be written exactly as the string
pause